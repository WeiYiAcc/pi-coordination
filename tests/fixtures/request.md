# Test Request - Needs Full Scoping

Add a simple counter feature to the app.

The counter should:
- Start at zero
- Have increment and decrement buttons
- Display the current count
- Persist the count between page reloads

Keep it minimal - just want to see if the basic flow works.
